import React from "react";
import ReactDOM from "react-dom/client";
import EventSpacesTracker from "./baseball-tracker.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <EventSpacesTracker />
  </React.StrictMode>
);
