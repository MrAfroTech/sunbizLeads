import React from "react";
import ReactDOM from "react-dom/client";
import FootballTracker from "./football-tracker.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FootballTracker />
  </React.StrictMode>
);
