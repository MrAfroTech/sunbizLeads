import React from "react";
import ReactDOM from "react-dom/client";
import LinkedInTriage from "./linkedin-triage.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LinkedInTriage />
  </React.StrictMode>
);
